<view class="screen catch-log">
  <view class="title">记一笔渔获</view>
  <view class="snapshot">系统已自动记录: {{snapshotText}}</view>

  <view class="section">
    <view class="section-title">鱼种和数量</view>
    <view class="species-row" wx:for="{{speciesRows}}" wx:key="name">
      <view>{{item.name}}</view>
      <view class="counter">
        <button data-name="{{item.name}}" data-delta="-1" bindtap="changeSpecies">-</button>
        <text>{{item.count}}</text>
        <button data-name="{{item.name}}" data-delta="1" bindtap="changeSpecies">+</button>
      </view>
    </view>
  </view>

  <view class="section">
    <view class="section-title">用饵</view>
    <view class="chips">
      <button wx:for="{{baitOptions}}" wx:key="*this" data-bait="{{item}}" bindtap="selectBait" class="{{form.bait === item ? 'active' : ''}}">{{item}}</button>
    </view>
  </view>

  <view class="section">
    <view class="section-title">照片</view>
    <view class="privacy">📵 照片只保存在你的手机本地，不会上传服务器</view>
    <view class="photos">
      <view class="photo" wx:for="{{form.photo_local_paths}}" wx:key="*this">
        <image src="{{item}}" mode="aspectFill"></image>
        <button data-index="{{index}}" bindtap="removePhoto">删除</button>
      </view>
      <button class="photo-add" bindtap="choosePhoto" wx:if="{{form.photo_local_paths.length < 9}}">+ 拍照</button>
    </view>
  </view>

  <view class="section">
    <view class="section-title">备注</view>
    <textarea value="{{form.note}}" bindinput="updateNote" placeholder="水色、流速、钓法变化等"></textarea>
  </view>

  <button class="submit" loading="{{saving}}" bindtap="submit">保存并生成渔获卡</button>
  <view class="status">{{statusMessage}}</view>
</view>
